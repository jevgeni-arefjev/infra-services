---
  - name: Initial setup
    hosts: all
    roles:
      - init
    gather_facts: yes
    tags:
      - always

  - name: DNS servers
    hosts: dns_servers
    roles:
      - bind
    tags:
      - b

  - name: Database servers
    hosts: db_servers
    roles:
      - mysql
    tags:
      - d
      - sw

  - name: Web servers
    hosts: web_servers
    roles:
      - docker
      - agama
#      - uwsgi
      - nginx
    tags:
      - w
      - sw

  - name: Prometheus
    hosts: prometheus
    roles:
      - nginx
      - prometheus
    tags:
      - p

  - name: Grafana
    hosts: grafana
    roles:
      - docker
      - grafana
    tags:
      - g
  
  - name: Loki
    hosts: loki
    roles:
      - loki
    tags:
      - l

  - name: HA
    hosts: ha
    roles:
      - keepalived
      - haproxy
    tags:
      - ha
