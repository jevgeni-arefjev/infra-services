#- name: Ansible ping module
#  ansible.builtin.ping:

- name: Update APT cache
  ansible.builtin.apt:
    cache_valid_time: 86400

- name: change DNS resolv.conf
  ansible.builtin.template:
    src: resolv.conf.j2
    dest: /etc/resolv.conf

- name: install prometheus node exporter
  ansible.builtin.apt:
    name: prometheus-node-exporter

- name: Install unzip package
  ansible.builtin.apt:
    name: unzip

- name: Download Promtail binary
  ansible.builtin.get_url:
    url: "https://github.com/grafana/loki/releases/download/v3.6.2/promtail-linux-amd64.zip"
    dest: /usr/bin/promtail.zip

- name: Unzip Promtail binary
  ansible.builtin.unarchive:
    src: /usr/bin/promtail.zip
    dest: /usr/bin/
    remote_src: yes

- name: Create directory for Promtail
  ansible.builtin.file:
    path: "{{ item }}"
    state: directory
  loop:
    - /etc/promtail
    - /var/lib/promtail

- name: Create Promtail user
  ansible.builtin.user:
    name: promtail
    system: yes

- name: Add promtail user to adm group for syslog access
  ansible.builtin.user:
    name: promtail
    groups: adm
    append: yes

- name: Install Promtail configuration
  ansible.builtin.copy:
    src: promtail-local-config.yaml
    dest: /etc/promtail/promtail-local-config.yaml
  notify: Restart Promtail

- name: Create Promtail systemd service
  ansible.builtin.copy:
    src: promtail.service
    dest: /etc/systemd/system/promtail.service

- name: Promtail Service
  ansible.builtin.service:
    name: promtail
    state: started
    enabled: true

- name: Add user backup
  ansible.builtin.user:
    name: backup
    create_home: yes
    home: /home/backup
    generate_ssh_key: yes
    ssh_key_file: .ssh/id_rsa

- name: Add backup server public key 
  ansible.builtin.template:
    src: known_hosts.j2
    dest: /home/backup/.ssh/known_hosts
    owner: backup
    group: backup

- name: Create backup restore directory
  ansible.builtin.file:
    path: /home/backup/restore
    state: directory
    owner: backup
    mode: '700'

- name: Install Duplicity
  ansible.builtin.apt:
    name: duplicity

# needed for creating DNS repords with community.general.nsupdate
- name: Install dnspython package
  ansible.builtin.apt:
    name: python3-dnspython

#- name: Add user juri
#  ansible.builtin.user:
#    name: juri
#    create_home: yes

#- name: Add user roman
#  ansible.builtin.user:
#    name: roman
#    create_home: yes

#- name: Set authorized keys for juri
#  ansible.posix.authorized_key:
#    user: juri
#    state: present
#    key: https://github.com/hudolejev.keys

#- name: Set authorized keys for roman
#  ansible.posix.authorized_key:
#    user: roman
#    state: present
#    key: https://github.com/romankuchin.keys

